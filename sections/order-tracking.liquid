<article class="page-content section-padding">
  <div class="page-content__inner">
    <h1 class="page-content__title">وين طلبج؟</h1>
    <div class="page-content__body rte">
      {% if page.content != blank %}
        {{ page.content }}
      {% else %}
        <p>تقدرين تتابعين طلبج من حسابج أو عبر واتساب. رقّم الطلب يوصلج بالإيميل بعد الشراء.</p>
        <p><a href="{{ routes.account_url }}">دخول حسابج</a></p>
      {% endif %}
    </div>
  </div>
</article>

{% stylesheet %}
  .page-content__inner { max-width: 48rem; margin-inline: auto; padding-inline: var(--page-margin); line-height: 1.8; }
  .page-content__title { font-size: clamp(1.5rem, 4vw, 2.25rem); font-weight: 700; margin-block-end: 1.5rem; color: var(--color-dark); }
{% endstylesheet %}

{% schema %}
{
  "name": "Order tracking",
  "class": "order-tracking-section",
  "settings": []
}
{% endschema %}
