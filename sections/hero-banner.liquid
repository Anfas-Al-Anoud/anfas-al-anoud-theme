<section class="hero-banner full-width" data-animate-section>
  <div class="hero-banner__frame">
    <div class="hero-banner__media" aria-hidden="true">
      {% if section.settings.image_desktop %}
        {% render 'image',
          image: section.settings.image_desktop,
          alt: section.settings.image_alt,
          fallback_alt: section.settings.heading,
          class: 'hero-banner__image hero-banner__image--desktop',
          width: 1920,
          loading: 'eager'
        %}
      {% else %}
        {% liquid
          assign desktop_asset = section.settings.image_desktop_asset | default: 'hero-banner-desktop.jpg'
        %}
        {% render 'theme-asset-image',
          asset: desktop_asset,
          alt: section.settings.image_alt,
          class: 'hero-banner__image hero-banner__image--desktop',
          width: 1920,
          loading: 'eager'
        %}
      {% endif %}
      {% if section.settings.image_mobile %}
        {% render 'image',
          image: section.settings.image_mobile,
          alt: section.settings.image_alt,
          fallback_alt: section.settings.heading,
          class: 'hero-banner__image hero-banner__image--mobile',
          width: 900,
          loading: 'eager'
        %}
      {% elsif section.settings.image_desktop %}
        {% render 'image',
          image: section.settings.image_desktop,
          alt: section.settings.image_alt,
          fallback_alt: section.settings.heading,
          class: 'hero-banner__image hero-banner__image--mobile',
          width: 900,
          loading: 'eager'
        %}
      {% else %}
        {% liquid
          assign mobile_asset = section.settings.image_mobile_asset | default: 'hero-banner-mobile.jpg'
        %}
        {% render 'theme-asset-image',
          asset: mobile_asset,
          alt: section.settings.image_alt,
          class: 'hero-banner__image hero-banner__image--mobile',
          width: 900,
          loading: 'eager'
        %}
      {% endif %}
      <div class="hero-banner__veil"></div>
    </div>

    <div class="hero-banner__content">
      {% if section.settings.eyebrow != blank %}
        <p class="hero-banner__eyebrow">{{ section.settings.eyebrow }}</p>
      {% else %}
        <p class="hero-banner__eyebrow">أنفاس العنود</p>
      {% endif %}
      <h1 class="hero-banner__heading">{{ section.settings.heading }}</h1>
      <p class="hero-banner__text">{{ section.settings.text }}</p>
      {% if section.settings.button_label != blank %}
        <a href="{{ section.settings.button_link | default: routes.all_products_collection_url }}" class="hero-banner__btn">
          {{ section.settings.button_label }}
        </a>
      {% endif %}
    </div>
  </div>
</section>

{% stylesheet %}
  .hero-banner {
    padding-block: clamp(1rem, 3vw, 2rem);
    padding-inline: var(--page-margin);
  }

  .hero-banner__frame {
    position: relative;
    max-width: var(--page-width);
    margin-inline: auto;
    border-radius: calc(var(--style-border-radius-cards) + 4px);
    overflow: hidden;
    box-shadow: 0 24px 60px rgba(61, 43, 31, 0.18);
  }

  .hero-banner__media {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .hero-banner__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .hero-banner__image--mobile { display: block; }
  .hero-banner__image--desktop { display: none; }

  .hero-banner__veil {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(270deg, rgba(94, 20, 31, 0.92) 0%, rgba(122, 31, 43, 0.6) 45%, rgba(122, 31, 43, 0.12) 100%),
      radial-gradient(circle at 90% 15%, rgba(201, 162, 76, 0.32), transparent 55%);
  }

  .hero-banner__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    text-align: start;
    min-height: clamp(17rem, 52vh, 30rem);
    padding: clamp(1.75rem, 6vw, 4.5rem);
    color: var(--color-white);
  }

  .hero-banner__eyebrow {
    margin: 0 0 0.75rem;
    font-size: 0.85rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-gold);
    font-weight: 700;
  }

  .hero-banner__heading {
    margin: 0 0 1rem;
    font-size: clamp(2rem, 6vw, 3.25rem);
    font-weight: 800;
    line-height: 1.2;
    text-wrap: balance;
  }

  .hero-banner__text {
    margin: 0 0 1.75rem;
    font-size: clamp(1rem, 2.4vw, 1.2rem);
    line-height: 1.9;
    color: rgba(255, 255, 255, 0.92);
    max-width: 32rem;
  }

  .hero-banner__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.85rem 1.6rem;
    border-radius: var(--style-border-radius-buttons, 999px);
    background: var(--color-gold);
    color: var(--color-white);
    font-weight: 700;
    text-decoration: none;
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.22);
  }

  /* —— Commerce hero (IMG_7838): lifestyle image + bottom-start copy + rectangular CTA —— */
  .header-style--commerce .hero-banner {
    padding: 0;
    background: #f6f4f1;
  }
  .header-style--commerce .hero-banner__frame {
    max-width: none;
    width: 100%;
    border-radius: 0;
    box-shadow: none;
    min-height: clamp(28rem, 78vh, 44rem);
  }
  .header-style--commerce .hero-banner__veil {
    background:
      linear-gradient(0deg, rgba(255, 255, 255, 0.55) 0%, rgba(255, 255, 255, 0.08) 42%, transparent 70%);
  }
  .header-style--commerce .hero-banner__content {
    justify-content: flex-end;
    padding: 1.5rem var(--page-margin) 2rem;
    color: var(--color-primary);
    max-width: 18rem;
  }
  .header-style--commerce .hero-banner__eyebrow {
    color: var(--color-primary);
    font-size: 1.65rem;
    letter-spacing: 0.02em;
    font-weight: 800;
    margin-block-end: 0.15rem;
  }
  .header-style--commerce .hero-banner__heading {
    color: var(--color-primary);
    font-size: clamp(1.15rem, 4.5vw, 1.45rem);
    font-weight: 700;
    margin-block-end: 0.85rem;
  }
  .header-style--commerce .hero-banner__text {
    display: none;
  }
  .header-style--commerce .hero-banner__btn {
    border-radius: 0.2rem;
    background: var(--color-primary);
    color: #fff;
    padding: 0.7rem 1.35rem;
    font-size: 0.78rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    box-shadow: none;
    min-width: 9.5rem;
  }

  /* —— Overlay hero (IMG_7839): tall image-only, no copy —— */
  .header-style--overlay.template-index .hero-banner {
    padding: 0;
    background: #f3f1ee;
  }
  .header-style--overlay.template-index .hero-banner__frame {
    max-width: none;
    width: 100%;
    border-radius: 0;
    box-shadow: none;
    min-height: 100svh;
    min-height: 100vh;
  }
  .header-style--overlay.template-index .hero-banner__veil {
    display: none;
  }
  .header-style--overlay.template-index .hero-banner__content {
    display: none;
  }
  .header-style--overlay.template-index .hero-banner__image {
    object-position: center top;
  }

  @media (max-width: 767px) {
    .header-style--classic .hero-banner {
      padding: 0;
    }
    .header-style--classic .hero-banner__frame {
      max-width: none;
      width: 100%;
      border-radius: 0;
      box-shadow: none;
      aspect-ratio: 1.7 / 1;
      min-height: 14.5rem;
    }
    .header-style--classic .hero-banner__veil {
      background:
        linear-gradient(90deg, rgba(94, 20, 31, 0.88) 0%, rgba(94, 20, 31, 0.45) 55%, rgba(94, 20, 31, 0.15) 100%),
        linear-gradient(0deg, rgba(61, 43, 31, 0.55) 0%, transparent 50%);
    }
    .header-style--classic .hero-banner__content {
      position: absolute;
      inset: 0;
      z-index: 2;
      min-height: 0;
      justify-content: center;
      align-items: flex-start;
      padding: 0.75rem var(--page-margin);
      max-width: 72%;
    }
    .header-style--classic .hero-banner__eyebrow {
      font-size: 0.625rem;
      margin-block-end: 0.25rem;
    }
    .header-style--classic .hero-banner__heading {
      font-size: clamp(1.0625rem, 4.2vw, 1.25rem);
      margin-block-end: 0.5rem;
      line-height: 1.3;
    }
    .header-style--classic .hero-banner__text { display: none; }
    .header-style--classic .hero-banner__btn {
      padding: 0.4375rem 0.875rem;
      font-size: 0.75rem;
    }

    .header-style--commerce .hero-banner__frame {
      min-height: 72vh;
      aspect-ratio: auto;
    }
    .header-style--commerce .hero-banner__content {
      position: absolute;
      inset: auto 0 0 0;
      padding: 1.25rem 1.1rem 1.5rem;
      max-width: 70%;
    }
    .header-style--commerce .hero-banner__eyebrow {
      font-size: 1.5rem;
    }
    .header-style--commerce .hero-banner__heading {
      font-size: 1.05rem;
    }
  }

  @media (min-width: 768px) {
    .hero-banner__image--mobile { display: none; }
    .hero-banner__image--desktop { display: block; }

    .header-style--classic .hero-banner__frame {
      max-height: 80vh;
      min-height: 28rem;
    }
    .header-style--classic .hero-banner__content {
      min-height: 100%;
      max-width: 34rem;
    }

    .header-style--commerce .hero-banner__frame {
      min-height: min(80vh, 46rem);
    }
    .header-style--commerce .hero-banner__content {
      max-width: 22rem;
      padding-inline-start: clamp(2rem, 6vw, 4rem);
      padding-block-end: 3rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.hero_banner.name",
  "class": "hero-banner-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image_desktop",
      "label": "صورة سطح المكتب (hero_banner_desktop.jpg)"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "صورة الجوال (hero_banner_mobile.jpg)"
    },
    {
      "type": "text",
      "id": "image_desktop_asset",
      "label": "ملف صورة الديسكتوب (assets/)",
      "default": "hero-banner-desktop.jpg",
      "info": "صورة سياقية من مجلد assets — ليست صورة منتج."
    },
    {
      "type": "text",
      "id": "image_mobile_asset",
      "label": "ملف صورة الجوال (assets/)",
      "default": "hero-banner-mobile.jpg"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "نص alt للصورة (مطلوب)",
      "default": "صورة للمنتجات من الأعلى - أنفاس العنود",
      "info": "مطلوب لإمكانية الوصول ومحركات البحث. لا تتركي الحقل فارغًا."
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "سطر فوق العنوان (مثل NEW)",
      "default": "جديد"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "العنوان",
      "default": "عيشي جوج.. وخلج غير عنهم"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "النص",
      "default": "تميزي بخلطات ما تتقلد، تعطيج حضورًا قويًا وتلفت الانتباه"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "نص الزر",
      "default": "تسوقي الآن"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "رابط الزر"
    }
  ],
  "presets": [{ "name": "t:sections.hero_banner.name" }]
}
{% endschema %}
