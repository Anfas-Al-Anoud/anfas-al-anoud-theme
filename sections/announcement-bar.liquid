<div class="announcement-bar" role="region" aria-label="{{ 'sections.announcement_bar.name' | t }}">
  <div class="announcement-bar__track">
    <p class="announcement-bar__text">{{ section.settings.text }}</p>
    <p class="announcement-bar__text" aria-hidden="true">{{ section.settings.text }}</p>
  </div>
</div>

{% stylesheet %}
  .announcement-bar {
    background: var(--color-dark);
    color: var(--color-sand);
    overflow: hidden;
    padding-block: 0.625rem;
  }
  .announcement-bar__track {
    display: flex;
    width: max-content;
    animation: marquee 30s linear infinite;
  }
  .announcement-bar__text {
    white-space: nowrap;
    padding-inline: 3rem;
    font-size: 0.875rem;
    margin: 0;
  }
  @keyframes marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  @media (prefers-reduced-motion: reduce) {
    .announcement-bar__track { animation: none; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.announcement_bar.name",
  "class": "announcement-bar-section",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "النص",
      "default": "استغلي وقتج الذهبي.. توصيل مجاني اليوم لعيونج – عروض عيد الأضحى... ما عيبج المنتج؟ ما تحاتين رديه خلال 3 أيام"
    }
  ]
}
{% endschema %}
